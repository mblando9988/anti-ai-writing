The migration ran fine in staging because staging has 2000 rows and production has 90 million. The ALTER TABLE took a lock and held it for 40 minutes, and everything upstream timed out. Next time we use the online migration tool, or we do it in two deploys: add the nullable column, backfill in batches, then add the constraint.