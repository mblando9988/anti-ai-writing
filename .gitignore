# macOS
.DS_Store
# throwaway workspace
scratch/
# build artifacts (compiled hook — rebuild with: swiftc -O src/anti_ai_sem.swift -o bin/anti_ai_sem)
bin/anti_ai_sem
.build/
# python
__pycache__/
*.pyc
bin/embed_one

# portable engine vector cache (regenerated from the corpus)
corpus/.portable_cache.json
corpus/.portable_cache.bin

# transient generation staging (merged into the corpus, then deleted)
undefined/
